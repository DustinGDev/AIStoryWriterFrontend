export default {
    render() {
        return (
            <nav>
                <router-link to="/dashboard">Dahboard</router-link>
                <router-link to="/competitions">Competitions</router-link>
            </nav>
        )
    }
}